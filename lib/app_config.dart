class AppConfig {
  /// Basculer à false pour la version complète
  static const bool isDemo = true;

  // Limites de la version démo
  static const int maxOrganizationsDemo = 1;
  static const int maxAuditsPerOrgDemo = 1;

  // Export autorisé ou pas
  static const bool demoDisableExport = true;
}
